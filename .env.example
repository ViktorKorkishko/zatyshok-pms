# Copy to .env (git-ignored) and adjust if needed.
# Used by docker-compose.yml; the API takes the password from
# user-secrets or the DB_PASSWORD environment variable (see README).
POSTGRES_USER=zatyshok
POSTGRES_PASSWORD=zatyshok_dev
